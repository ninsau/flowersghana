type Bouquets @model @auth(rules: [{allow: public}]) {
  id: ID!
  img: String
  title: String
  description: String
  availability: String
  amount: Int
  slug: String
  tags: String
  category: String
}
 